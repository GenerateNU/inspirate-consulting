# Backend

## Fiber


## Backend Flow
We get an HTTP request from our front end ->
Hits Fiber HTTP Server ->
Goes to Huma Route ->
Context and params get passed down to Handler ->
Repository will interact with the DB ->
Supabase DB, SQL